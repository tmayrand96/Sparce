from pathlib import Path

def extract_raw_text(image_path: Path) -> str:
    """Ingests a mobile camera image and extracts unformatted raw string text."""
    # TODO: Implement Tesseract / Vision API orchestration
    return "[Mock Raw OCR Text from Image]"